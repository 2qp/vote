import 'package:flutter/material.dart';

class VotingUi extends StatelessWidget {
  const VotingUi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
